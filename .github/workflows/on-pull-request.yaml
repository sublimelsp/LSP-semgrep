name: Check configuration schema changes

on:
  pull_request:
    paths:
      - 'semgrep/pyproject.toml'

permissions:
  pull-requests: write
  repository-projects: write
  contents: write

jobs:
  update-settings:
    uses: sublimelsp/workflows/.github/workflows/update-settings.yaml@v2
